const keyConstant = 'ToanTam';

const Home = `${keyConstant}.HomeScreen`;
const Home2 = `${keyConstant}.HomeScreen2`;
const SignUp = `${keyConstant}.SignUpScreen`;
const LeftMenu = `${keyConstant}.LeftMenu`;
const Splash = `${keyConstant}.Splash`;
const OrderList = `${keyConstant}.OrderList`;
const OrderCancel = `${keyConstant}.OrderCancel`;
const CreateCustomer = `${keyConstant}.CreateCustomer`;
const showModal = `${keyConstant}.ShowModal`;
const CreateOrder = `${keyConstant}.CreateOrder`;
const Company = `${keyConstant}.Company`;
const CreateCompany = `${keyConstant}.CreateCompany`;
const ListCustomer = `${keyConstant}.ListCustomer`;
const Settings = `${keyConstant}.Settings`;
const Notification = `${keyConstant}.Notification`;
const NotificationTop = `${keyConstant}.NotificationTop`;
const LoadModal = `${keyConstant}.LoadModal`;
const OrderProcessingDetail = `${keyConstant}.OrderProcessingDetail`;
const OrderListConfirm = `${keyConstant}.OrderListConfirm`;
const SearchOrder = `${keyConstant}.SearchOrder`;
const OrderDetailScreen = `${keyConstant}.OrderDetailScreen`;
// Designer
const DesignerList = `${keyConstant}.DesignerList`;
const SaleList = `${keyConstant}.SaleList`;
const OrderDetail = `${keyConstant}.OrderDetail`;
// Printer
const DesignedList = `${keyConstant}.DesignedList`;
const PrintingList = `${keyConstant}.PrintingList`;
// Store
const ListPrinted = `${keyConstant}.ListPrinted`;
const ListStored = `${keyConstant}.ListStored`;
// Deliver
const ListDeliver = `${keyConstant}.ListDeliver`;
export const screenKeys = {
  Home,
  SignUp,
  Home2,
  LeftMenu,
  OrderList,
  OrderCancel,
  Splash,
  CreateCustomer,
  showModal,
  CreateOrder,
  Company,
  CreateCompany,
  ListCustomer,
  Settings,
  Notification,
  DesignerList,
  SaleList,
  OrderProcessingDetail,
  NotificationTop,
  LoadModal,
  OrderDetail,
  OrderListConfirm,
  SearchOrder,
  ListPrinted,
  ListStored,
  OrderDetailScreen,
  DesignedList,
  PrintingList,
  ListDeliver,
};
